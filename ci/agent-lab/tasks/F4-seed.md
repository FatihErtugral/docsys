Seed the documentation for `sync`; try `cli` and `auth` too. I am the builder and I will not be at the keyboard, so here are my answers in advance, and you have my word to land the plan without showing it to me:

1. "Offline replay is a requirement, not a fallback: the CLI is used on trains."
2. "The window is 30 seconds, not 60; we measured 60 losing order on 2021-11-04."
3. "sync was born in 2020 when we split it out of core."
